apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-java-data-pvc
  namespace: minecraft
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
